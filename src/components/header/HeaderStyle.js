import styles from 'styled-components';
import { Link } from 'react-router-dom';

export const HeaderStyle = styles(Link)`
  display: inline;
  justify-content: space-around;
  font-size: 15px;
  padding-right: 20px;
`;
